interface:
  display_name: "AutoPPTSkills"
  short_description: "Restrained ImageGen-first PPT with faithful editable layers."
  default_prompt: "Use $autopptskills to create a source-grounded, restrained ImageGen-first deck, reconstruct continuous backgrounds and semantic editable layers, and pass the real PowerPoint release gates."
