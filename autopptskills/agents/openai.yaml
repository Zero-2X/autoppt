interface:
  display_name: "AutoPPTSkills"
  short_description: "Restrained ImageGen-first PPT with faithful editable layers."
  default_prompt: "Use $autopptskills to create a source-grounded ImageGen-first deck: lock evidence and style, generate one complete built-in ImageGen master per slide, reconstruct honest semantic layers, and do not claim Gold release until real PowerPoint and full-slide review gates pass."
